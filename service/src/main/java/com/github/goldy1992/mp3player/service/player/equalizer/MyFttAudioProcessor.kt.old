package com.github.goldy1992.mp3player.service.dagger.modules.service

import com.google.android.exoplayer2.audio.AudioProcessor
import com.google.android.exoplayer2.audio.BaseAudioProcessor
import com.google.android.exoplayer2.audio.DefaultAudioSink
import com.google.android.exoplayer2.audio.DefaultAudioTrackBufferSizeProvider
import java.nio.ByteBuffer

class MyFttAudioProcessor() : BaseAudioProcessor() {
    val defaultAudioTrackBufferSizeProvider = DefaultAudioTrackBufferSizeProvider.Builder().build()

    override fun queueInput(inputBuffer: ByteBuffer) {
        defaultAudioSink.get
        TODO("Not yet implemented")
    }

    override fun onConfigure(inputAudioFormat: AudioProcessor.AudioFormat): AudioProcessor.AudioFormat {
        return super.onConfigure(inputAudioFormat)
    }
}