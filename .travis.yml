language: android
android:
  components:
  - build-tools-28.0.3
  - android-28
  - add-on
  - extra
  licenses:
  - 'android-sdk-license-.+'
  - 'google-gdk-license-.+'
sudo: required
jdk: oraclejdk8
before_install:
 - chmod +x gradlew
 - yes | sdkmanager "platforms;android-28"
script:
  - ./gradlew clean build